const saveConfigureSocialProSettings = ({ commit }, payload) => {
    // To-Do: api call to corresponding endpoint
    return new Promise(resolve => setTimeout(resolve, 100))
}

export default {
    saveConfigureSocialProSettings
}
